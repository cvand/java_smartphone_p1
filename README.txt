My model is implemented in that so that I don't need to instantiate 
all the elements in the optset and opt arrays. In fact I keep a variable
for knowing the actual size of the arrays, meaning the number of 
non-null elements in the arrays. 